<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <!-- Match what Confluent’s pom.xml expects -->
    <groupId>com.github.zacharydhamilton</groupId>
    <artifactId>ride4ever</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>ride4ever</name>
    <description>PoC dependency confusion package for HackerOne report</description>

    <build>
        <plugins>
            <!-- JitPack needs this to build a JAR -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.8.1</version>
                <configuration>
                    <source>1.8</source>
                    <target>1.8</target>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
